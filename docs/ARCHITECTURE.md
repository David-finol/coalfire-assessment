# Architecture Diagram

## High-Level Infrastructure Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                          Internet (0.0.0.0/0)                       │
├─────────────────────────────────────────────────────────────────────┤
│                         Internet Gateway                             │
└─────────────────────┬───────────────────────────────────┬───────────┘
                      │                                   │
        ┌─────────────┴───────────────┬───────────────────┴──────┐
        │                             │                          │
┌───────▼────────┐          ┌────────▼─────────┐      ┌──────────▼──────┐
│  Management    │          │  Application     │      │  Application    │
│  Subnet (AZ1)  │          │  Subnet (AZ1)    │      │  Subnet (AZ2)   │
│ 10.1.3.0/24    │          │  10.1.1.0/24     │      │  10.1.2.0/24    │
└────┬────────┬──┘          └─────┬──────┬─────┘      └────┬──────┬────┘
     │        │                   │      │                │      │
     │    ┌───▼───┐           ┌───▼──┐  │            ┌───▼──┐  │
     │    │ NAT   │           │  EC2  │  │            │  EC2  │  │
     │    │GW (AZ1)           │(ASG)  │  │            │(ASG)  │  │
     │    └───────┘           └──┬──┬┘  │            └───┬───┘  │
     │                           │  │   │                │      │
     │    ┌──────────────┐       │  │   │           ┌────▼──┐   │
     │    │  Management  │       │  │   │           │ NAT   │   │
     │    │     EC2      │       │  │   │           │GW(AZ2)│   │
     │    └──────────────┘       │  │   │           └───────┘   │
     │                           │  │   │                │      │
└─────┼───────────────────────┬──┴──┴───┴────────────────┴──────┘
      │                       │
      │                  ┌────▼─────┐
      │                  │    ALB    │
      │                  │ (Public)  │
      │                  └────┬──────┘
      │                       │
      │                  ┌────▼──────────────────┐
      │                  │   Target Group        │
      │                  │  (EC2 ASG instances)  │
      │                  └───────────────────────┘
      │
      └── SSH (Restricted IP)
```

## Security Group Rules

```
┌─────────────────────────────────────────────────────────────────┐
│                       ALB Security Group                          │
├─────────────────────────────────────────────────────────────────┤
│ Inbound:                                                          │
│  - TCP 80 from 0.0.0.0/0 (HTTP)                                  │
│  - TCP 443 from 0.0.0.0/0 (HTTPS)                                │
│ Outbound:                                                         │
│  - All traffic allowed                                            │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                   ASG Security Group                              │
├─────────────────────────────────────────────────────────────────┤
│ Inbound:                                                          │
│  - TCP 22 from Management SG (SSH)                                │
│  - TCP 80 from ALB SG (HTTP)                                      │
│ Outbound:                                                         │
│  - All traffic allowed                                            │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│              Management Security Group                            │
├─────────────────────────────────────────────────────────────────┤
│ Inbound:                                                          │
│  - TCP 22 from specific IP/CIDR only (SSH)                        │
│ Outbound:                                                         │
│  - All traffic allowed                                            │
└─────────────────────────────────────────────────────────────────┘
```

## Network Segmentation

```
VPC: 10.1.0.0/16
│
├── Public Tier (Internet-facing)
│   └── Management Subnet: 10.1.3.0/24 (AZ1)
│       └── Management EC2 (Bastion/Jump Host)
│           ├── Public IP: Dynamic
│           ├── SSH: Restricted to specific IPs
│           └── Can SSH to application instances
│
├── Application Tier (Private, outbound internet)
│   ├── Application Subnet: 10.1.1.0/24 (AZ1)
│   │   └── ASG EC2 Instances (2-6 capacity)
│   │       ├── Private IPs only
│   │       ├── Internet access via NAT GW (AZ1)
│   │       └── SSH via Management bastion
│   │
│   └── Application Subnet: 10.1.2.0/24 (AZ2)
│       └── ASG EC2 Instances (2-6 capacity)
│           ├── Private IPs only
│           ├── Internet access via NAT GW (AZ2)
│           └── SSH via Management bastion
│
└── Backend Tier (Private, no internet)
    ├── Backend Subnet: 10.1.4.0/24 (AZ1)
    │   └── Reserved for databases, caches
    │
    └── Backend Subnet: 10.1.5.0/24 (AZ2)
        └── Reserved for databases, caches
```

## Data Flow Diagram

### Incoming Request Flow

```
Internet User (203.0.113.100)
         │
         ├─ HTTP Request to ALB DNS
         │
         ▼
┌─────────────────────────┐
│   Application Load      │
│   Balancer (ALB)        │
│                         │
│ - Receives on port 80   │
│ - Distributes traffic   │
│ - Health checks targets │
└────────────┬────────────┘
             │
   ┌─────────┴────────┬────────────┐
   │                  │            │
   ▼                  ▼            ▼
┌────────┐       ┌────────┐   ┌────────┐
│ ASG    │       │ ASG    │   │ ASG    │
│ EC2-1  │       │ EC2-2  │   │ EC2-3  │
│        │       │        │   │        │
│Apache  │       │Apache  │   │Apache  │
│port 80 │       │port 80 │   │port 80 │
│        │       │        │   │        │
│Return  │       │Return  │   │Return  │
│response│       │response│   │response│
└────┬───┘       └───┬────┘   └────┬───┘
     │               │             │
     └───────┬───────┴─────────────┘
             │
             ▼
        Response to User
```

### SSH Access Flow

```
Admin (203.0.113.100) with restricted CIDR
         │
         ├─ SSH to Management EC2 (public IP)
         │
         ▼
┌──────────────────────────┐
│ Management EC2           │
│ (Bastion/Jump Host)      │
│                          │
│ - Public IP: 203.0.113.1 │
│ - In Management Subnet   │
└────────┬─────────────────┘
         │
         ├─ SSH to ASG EC2 (private IP)
         │  via Security Group trust
         │
         ▼
┌──────────────────────────┐
│ ASG EC2 Instance         │
│ (Application Subnet)     │
│                          │
│ - Private IP: 10.1.x.x   │
│ - No public IP           │
└──────────────────────────┘
```

## High Availability

```
┌────────────────────────────────────────────────┐
│        Application Load Balancer              │
│      (Distributes across AZs)                 │
└────────┬──────────────────────┬───────────────┘
         │                      │
    ┌────▼────────┐        ┌────▼────────┐
    │  AZ1         │        │  AZ2        │
    │(us-east-1a) │        │(us-east-1b) │
    │              │        │             │
    │ ┌──────────┐ │        │ ┌─────────┐ │
    │ │ NAT GW   │ │        │ │ NAT GW  │ │
    │ │ (Public) │ │        │ │(Public) │ │
    │ └──────────┘ │        │ └─────────┘ │
    │              │        │             │
    │ ┌──────────┐ │        │ ┌─────────┐ │
    │ │ ASG EC2  │ │        │ │ASG EC2  │ │
    │ │1-3 inst. │ │        │ │1-3 inst.│ │
    │ │(Private) │ │        │ │(Private)│ │
    │ └──────────┘ │        │ └─────────┘ │
    │              │        │             │
    └──────────────┘        └─────────────┘
    
If AZ1 fails, traffic automatically
shifts to AZ2 instances via ALB
```

## Terraform Modules Dependency Graph

```
┌──────────────────────────────────┐
│       Dev Environment             │
│    (main.tf orchestrator)         │
└────────┬─────────────────────────┘
         │
    ┌────┼────┬────────┬──────┐
    │    │    │        │      │
    ▼    ▼    ▼        ▼      ▼
  ┌──┐┌──┐┌──────┐┌──────┐┌──┐
  │NW││SG││ALB  ││Compute││CT│
  └┬─┘└─┬┘└─┬───┘└──┬────┘└──┘
   │    │   │       │
   │    │   └───────┤
   │    └───────────┤
   └────────────────┤
Dependencies:       │
NW (Networking)    [Independent]
SG (Security)      [Requires: VPC ID from NW]
ALB                [Requires: VPC ID, SG IDs, Subnets from NW, SG]
Compute            [Requires: Subnets, SG IDs from NW, SG, Target Group from ALB]
```

## State Management Architecture

```
┌────────────────────────────────────────────────┐
│           Git Repository                        │
│     (Source of Truth for IaC)                  │
│                                                 │
│  - Terraform code                              │
│  - Variables and outputs                       │
│  - State backend configuration                 │
└────────────────────────────────────────────────┘
                    │
                    │ Push/Pull
                    │ (GitHub Actions CI/CD)
                    │
         ┌──────────▼──────────┐
         │  GitHub Actions     │
         │  (CI/CD Pipeline)   │
         │                     │
         │ 1. Validate         │
         │ 2. Plan             │
         │ 3. Apply            │
         └──────────┬──────────┘
                    │
                    │ Read/Write State
                    │ & Locks
                    │
    ┌───────────────┴────────────┬──────────────┐
    │                            │              │
    ▼                            ▼              ▼
┌─────────────┐         ┌──────────────┐  ┌──────────┐
│ S3 Bucket   │         │  DynamoDB    │  │   AWS   │
│ (State File)│────┼────│  (Locks)     │  │ Account  │
│             │    │    │              │  │   ID:    │
│- terraform. │    │    │- LockID: key │  │ 123456   │
│  tfstate    │    │    │- Expires:30s │  │789012    │
│- .tfbackup  │    │    │- On-demand   │  │          │
│- Versioning│    │    │  billing     │  │          │
│- Encryption│    │    │              │  │          │
│- MFA Delete│    │    │              │  │          │
│  (Optional)│    │    │              │  │          │
└─────────────┘    │    └──────────────┘  └──────────┘
                   │
             [Prevents conflicts]
             [One lock at a time]
```

---

For deployment instructions, see [DEPLOYMENT_GUIDE.md](DEPLOYMENT_GUIDE.md)
